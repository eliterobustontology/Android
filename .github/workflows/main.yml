name: Android Build (Release)

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Make Gradle Wrapper executable
        run: chmod +x gradlew

      - name: Ensure Android/app/keys directory exists
        run: mkdir -p Android/app/keys

      - name: Decode advance_native.pfx keystore to Android/app/keys folder
        run: echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 -d > Android/app/keys/advance_native.pfx

      - name: Build Signed AAB (Release)
        run: ./gradlew :app:bundleRelease --stacktrace

      - name: List AAB Output
        run: find app/build/outputs/bundle/release -name "*.aab"

      - name: Upload Signed AAB Artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-release-aab
          path: app/build/outputs/bundle/release/*.aab
